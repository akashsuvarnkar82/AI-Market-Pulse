name: AI Market Intelligence Monitor

on:
  schedule:
    # This is "Cron Syntax" - it means "Run every hour at minute 0"
    - cron: '0 * * * *'
  workflow_dispatch: # This adds a manual "Run" button in GitHub

jobs:
  monitor-and-analyze:
    runs-on: ubuntu-latest # Runs on a fresh Cloud Linux VM
    steps:
      - name: 🏁 Checkout Code
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 💿 Install Dependencies
        run: |
          pip install requests beautifulsoup4 transformers torch

      - name: 🚀 Run Intelligence Pipeline
        run: python engine/scraper.py # We will merge this with analyzer tomorrow!